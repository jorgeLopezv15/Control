import { header } from "../../Componentes/header/headerComponents.js";

let sectionCarrito = document.createElement('section');


sectionCarrito.appendChild(header());

document.body.appendChild(sectionCarrito);